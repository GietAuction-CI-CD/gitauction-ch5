name: Missing share data
on: push
jobs:
    share-data:
        runs-on: ubuntu-latest
        steps:
            - run: export RESULT="hello"        # 환경 변수를 이용해 다음스텝에 값 전달
            - run: echo "${RESULT}"             # 하지만 환경변수를 참조해도 값은 비어있다.